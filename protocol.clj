; learn protocol in clojure. 

; creating multi method. 
; (defmulti name-of-multi-method dispatch-function)
; this is how you create a method. 
; (defmethod name-of-multi-method match{match will check with the output of dispatch function. if output 
  ; matches the current match then this method will be implemented})


